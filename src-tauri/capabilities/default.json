{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the BIG Hat Entertainment shell. Intentionally minimal — we only add fine-grained perms when a real-world feature needs them. See lib.rs alpha.3 incident: invalid perm names panic the Builder before setup() runs, killing the windows-subsystem process silently.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default",
    "shell:default",
    "process:default",
    "dialog:default"
  ]
}
